using FiguresLibrary.Point;

namespace FiguresLibrary.Utils
{
    class CalculateNewPoint
    {
        public static void GetNewPoint(IPoint x, IPoint y, byte scale) {

            var a = new LibPoint(0, 0);
        }

    }
}
